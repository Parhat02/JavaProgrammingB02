package week10.day30_inheritance.day30Tasks.planets;

public class Mercury extends Planet{

    public Mercury(double mass, double radius, double surfaceGravity, double surfaceArea, double volume, long population) {
        super("Mercury", mass, radius, surfaceGravity, surfaceArea, volume, population);
    }
}
